# Install with: uv pip install -r requirements.txt

# Core analysis — needed by scripts/ (the reported finding)
pandas>=2.0.0
numpy>=1.26.0
statsmodels>=0.14.0
scipy>=1.11.0
matplotlib>=3.8.0

# Data ingestion. yfinance is needed only to refresh
# data/raw/ovx_vix_history.csv; the cached copy is committed, so
# scripts/ovx_placebo.py runs offline.
yfinance>=0.2.40
fredapi>=0.5.1
python-dotenv>=1.0.0

# Exploratory notebooks only
linearmodels>=6.0
seaborn>=0.13.0
jupyter>=1.0.0
ipykernel>=6.29.0
